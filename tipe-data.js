// Undefined, null, NaN, boolean, string, int, float, double, bigint

const arr = [10, "Adam", {nama:"adam"}, [1,2,3]]

const obj = {
    nama : "Adam",
    umur : "27",
    hobi : {
        makan : 7/10,
        minum : 7/10,
    }
}
console.log(arr)
console.log(obj)

console.log(obj.hobi.makan)